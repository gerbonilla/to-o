$('.form-wrapper').empty();
formWrapper = document.querySelector('.form-wrapper');
formWrapper.className += " margin-top-20-vh";
formWrapper.insertAdjacentHTML('afterbegin', '<%= j render "accidents", accidentContract: @accidentContract, accidents: @accidents, max_coverage: @max_coverage, highest_coverage: 0 %>');
