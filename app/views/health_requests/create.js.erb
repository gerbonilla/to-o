$('.page-wrapper').empty();
pageWrapper = document.querySelector('.page-wrapper');
// pageWrapper.className += " margin-top-20-vh";
pageWrapper.insertAdjacentHTML('afterbegin', '<%= j render "healthcontracts", health_request: @health_request, health_contracts: @health_contracts, max_coverage: @max_coverage, highest_coverage: 0 %>');
// $('.tap-target').tapTarget();
$('.modal').modal();
// $('.tap-target').tapTarget('open');
