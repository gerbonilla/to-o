$('.page-wrapper').empty();
pageWrapper = document.querySelector('.page-wrapper');
// formWrapper.className += "form-wrapper margin-top-20-vh";
pageWrapper.insertAdjacentHTML('afterbegin', '<%= j render "userinfo", health_request: @health_request, selectedPlan: @selectedPlan %>');
